import Menu from "../Menu";
import LoginPage from "../pages/LoginPage";

const Login = () => {
    return (
        <div>
            <Menu />
            <LoginPage />
        </div>
    );
};

export default Login;